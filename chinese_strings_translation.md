# Terjemahan String Cina dalam Kode Sumber

Berikut adalah kumpulan string bahasa Cina yang ditemukan dalam kode sumber proyek beserta terjemahan ke bahasa Indonesia:

## File: tests/test_signature.py
- `添加项目根目录到 Python 路径` → "Tambahkan direktori root proyek ke path Python"
- `示例用法` → "Contoh penggunaan"
- `生成的签名` → "Tanda tangan yang dihasilkan"
- `时间戳` → "Stempel waktu"

## File: tests/test_simple_signature.py
- `简单测试签名工具` → "Alat uji tanda tangan sederhana"
- `示例用法` → "Contoh penggunaan"
- `生成的签名` → "Tanda tangan yang dihasilkan"
- `时间戳` → "Stempel waktu"
- `签名函数测试通过！` → "Uji fungsi tanda tangan berhasil!"

## File: main.py
- `未找到可用 Token 且未启用匿名模式，服务可能无法正常工作` → "Token yang tersedia tidak ditemukan dan mode anonim tidak diaktifkan, layanan mungkin tidak berfungsi dengan normal"
- `应用正在关闭...` → "Aplikasi sedang ditutup..."
- `启动 {service_name} 服务...` → "Memulai layanan {service_name}..."
- `监听地址` → "Alamat pendengaran"
- `调试模式` → "Mode debug"
- `开启` → "Diaktifkan"
- `关闭` → "Dimatikan"
- `匿名模式` → "Mode anonim"
- `服务启动失败` → "Gagal memulai layanan"
- `收到中断信号，正在关闭服务...` → "Menerima sinyal interupsi, sedang menutup layanan..."

## File: app/templates/tokens.html
- `Token 管理` → "Manajemen Token"
- `管理和监控 AI 提供商的 Token` → "Mengelola dan memonitor Token penyedia AI"
- `批量验证` → "Verifikasi massal"
- `添加 Token` → "Tambah Token"
- `刷新列表` → "Segarkan daftar"
- `刷新` → "Segarkan"
- `提供商切换和操作栏` → "Beralih penyedia dan bilah operasi"
- `Token 列表` → "Daftar Token"
- `同步 Token 池` → "Sinkronisasi kumpulan Token"
- `健康检查` → "Pemeriksaan kesehatan"
- `添加 Token 弹窗` → "Tambah jendela Token"
- `添加 Token` → "Tambah Token"
- `Z.AI Token 验证` → "Verifikasi Token Z.AI"
- `匿名用户 Token (guest) 将被拒绝` → "Token pengguna anonim (guest) akan ditolak"
- `单个 Token` → "Token tunggal"
- `输入 Token（以 eyJ 开头的 JWT）` → "Masukkan Token (JWT yang dimulai dengan eyJ)"
- `批量导入` → "Impor massal"
- `批量导入（每行一个）` → "Impor massal (satu per baris)"
- `每行一个 Token，支持逗号分隔` → "Satu Token per baris, mendukung pemisahan koma"
- `支持格式：每行一个 Token，或使用逗号分隔` → "Format yang didukung: satu Token per baris, atau gunakan pemisahan koma"
- `取消` → "Batal"
- `添加` → "Tambah"
- `批量验证弹窗` → "Jendela verifikasi massal"
- `批量验证 Token` → "Verifikasi massal Token"
- `警告信息` → "Informasi peringatan"
- `将验证所有 <strong x-text="provider.toUpperCase()"></strong> Token 的有效性。此操作可能需要较长时间，请耐心等待。` → "Akan memverifikasi validitas semua Token <strong x-text="provider.toUpperCase()"></strong>. Operasi ini mungkin memakan waktu lama, mohon bersabar."
- `验证内容` → "Isi verifikasi"
- `检查 Token 是否有效` → "Periksa apakah Token valid"
- `识别 Token 类型（认证用户 / 匿名用户）` → "Identifikasi jenis Token (pengguna terautentikasi / pengguna anonim)"
- `更新数据库中的 Token 类型` → "Perbarui jenis Token dalam database"
- `匿名用户 Token 将被标记为不健康` → "Token pengguna anonim akan ditandai sebagai tidak sehat"
- `验证中...` → "Sedang diverifikasi..."
- `开始验证` → "Mulai verifikasi"
- `验证完成` → "Verifikasi selesai"

## File: app/templates/monitor.html
- `服务监控` → "Pemantauan layanan"
- `提供商状态详情` → "Detail status penyedia"
- `实时日志` → "Log real-time"
- `日志已清空` → "Log telah dikosongkan"
- `清空` → "Kosongkan"
- `加载中...` → "Sedang memuat..."

## File: app/utils/tool_call_handler.py
- `工具调用处理模块` → "Modul penanganan panggilan alat"
- `生成工具调用提示词` → "Hasilkan petunjuk panggilan alat"
- `将 OpenAI tools 定义转换为 Markdown 格式的说明文档` → "Konversi definisi alat OpenAI ke dokumen instruksi format Markdown"
- `Markdown 格式的工具使用说明` → "Instruksi penggunaan alat dalam format Markdown"
- `将工具定义注入到消息列表中` → "Suntikkan definisi alat ke daftar pesan"
- `工具提示已注入到消息列表,共 {len(processed)} 条消息` → "Petunjuk alat telah disuntikkan ke daftar pesan, total {len(processed)} pesan"
- `从响应内容中提取 tool_calls JSON` → "Ekstrak JSON tool_calls dari konten respons"
- `从 JSON 代码块中提取到 {len(tool_calls)} 个工具调用` → "Ekstrak {len(tool_calls)} panggilan alat dari blok JSON"
- `从内联 JSON 中提取到 {len(tool_calls)} 个工具调用` → "Ekstrak {len(tool_calls)} panggilan alat dari JSON sebaris"
- `从响应内容中移除工具调用 JSON` → "Hapus panggilan alat JSON dari konten respons"
- `内容清理完成,原始长度: {len(content)}, 清理后长度: {len(cleaned_result)}` → "Pembersihan konten selesai, panjang asli: {len(content)}, panjang setelah dibersihkan: {len(cleaned_result)}"
- `将消息内容转换为字符串` → "Konversi konten pesan ke string"

## File: app/templates/login.html
- `登录 - Z.AI2API 管理后台` → "Login - Backend Manajemen Z.AI2API"
- `Z.AI2API 管理后台` → "Backend Manajemen Z.AI2API"
- `请输入管理密码以继续` → "Silakan masukkan kata sandi manajemen untuk melanjutkan"
- `密码错误，请重试` → "Kata sandi salah, silakan coba lagi"
- `登录失败，请稍后重试` → "Login gagal, silakan coba lagi nanti"
- `密码` → "Kata sandi"
- `请输入管理密码` → "Silakan masukkan kata sandi manajemen"
- `登录` → "Login"
- `登录中...` → "Sedang login..."
- `默认密码：admin123（请在 .env 中修改 ADMIN_PASSWORD）` → "Kata sandi default: admin123 (silakan ubah ADMIN_PASSWORD di .env)"

## File: app/templates/index.html
- `仪表盘` → "Dashboard"
- `最后更新` → "Terakhir diperbarui"
- `运行时间` → "Waktu berjalan"
- `总请求数` → "Jumlah total permintaan"
- `成功率` → "Tingkat keberhasilan"
- `Token 池健康度` → "Kesehatan kumpulan Token"
- `个匿名` → "anonim"
- `Token 池状态` → "Status kumpulan Token"
- `最近请求日志` → "Log permintaan terbaru"
- `自动刷新` → "Segarkan otomatis"
- `加载中...` → "Sedang memuat..."

## File: app/templates/config.html
- `配置管理` → "Manajemen konfigurasi"
- `隐藏高级选项` → "Sembunyikan opsi lanjutan"
- `显示高级选项` → "Tampilkan opsi lanjutan"
- `基础配置` → "Konfigurasi dasar"
- `服务名称` → "Nama layanan"
- `显示在进程列表中的服务名称` → "Nama layanan yang ditampilkan dalam daftar proses"
- `监听端口` → "Port pendengaran"
- `服务监听的端口号` → "Nomor port yang didengarkan oleh layanan"
- `调试模式` → "Mode debug"
- `启用调试日志` → "Aktifkan log debug"
- `匿名模式` → "Mode anonim"
- `启用匿名模式（自动获取临时 Token）` → "Aktifkan mode anonim (dapatkan Token sementara secara otomatis)"
- `客户端认证密钥` → "Kunci autentikasi klien"
- `客户端访问本服务时使用的 API 密钥` → "Kunci API yang digunakan klien saat mengakses layanan ini"
- `跳过认证` → "Lewati autentikasi"
- `跳过客户端认证（仅开发环境）` → "Lewati autentikasi klien (hanya untuk lingkungan pengembangan)"
- `工具调用支持` → "Dukungan panggilan alat"
- `启用 Function Call（工具调用）功能` → "Aktifkan fungsi Function Call (panggilan alat)"
- `高级选项` → "Opsi lanjutan"
- `Token 失败阈值` → "Ambang kegagalan Token"
- `连续失败多少次后标记 Token 为失败状态` → "Berapa kali kegagalan berturut-turut sebelum menandai Token sebagai status gagal"
- `Token 恢复超时（秒）` → "Waktu habis pemulihan Token (detik)"
- `Token 失败后多久自动恢复（默认 1800 秒 / 30 分钟）` → "Berapa lama waktu Token gagal sebelum pulih otomatis (default 1800 detik / 30 menit)"
- `工具调用扫描限制（字符数）` → "Batas pemindaian panggilan alat (jumlah karakter)"
- `Function Call 功能扫描的最大字符数` → "Jumlah maksimum karakter yang dipindai oleh fungsi Function Call"
- `passport token（可选）` → "token passport (opsional)"
- `默认提供商` → "Penyedia default"
- `保存并重载` → "Simpan dan muat ulang"
- `重置` → "Atur ulang"
- `配置说明` → "Instruksi konfigurasi"
- `提示` → "Petunjuk"
- `配置保存后会自动热重载，大部分配置无需重启服务即可生效。仅 <code class="bg-blue-100 px-1 py-0.5 rounded">监听端口</code> 和 <code class="bg-blue-100 px-1 py-0.5 rounded">服务名称</code> 需要手动重启服务。` → "Setelah konfigurasi disimpan akan otomatis dimuat ulang secara panas, sebagian besar konfigurasi dapat berlaku tanpa perlu me-restart layanan. Hanya <code class="bg-blue-100 px-1 py-0.5 rounded">port pendengaran</code> dan <code class="bg-blue-100 px-1 py-0.5 rounded">nama layanan</code> yang perlu di-restart secara manual."
- `.env 文件预览` → "Pratinjau file .env"
- `刷新` → "Segarkan"
- `.env 文件预览` → "Pratinjau file .env"
- `加载中...` → "Sedang memuat..."
- `配置保存成功后的处理` → "Penanganan setelah konfigurasi berhasil disimpan"

## File: app/utils/token_pool.py
- `Token 池管理器 - 基于数据库的 Token 轮询和健康检查` → "Manajer kumpulan Token - Rotasi Token dan pemeriksaan kesehatan berbasis database"
- `Token 轮询机制 - 负载均衡和容错` → "Mekanisme rotasi Token - Load balancing dan toleransi kesalahan"
- `Z.AI 官方认证接口验证 - 基于 role 字段区分用户类型` → "Verifikasi antarmuka autentikasi resmi Z.AI - Membedakan jenis pengguna berdasarkan bidang peran"
- `Token 健康度监控 - 自动禁用失败 Token` → "Pemantauan kesehatan Token - Otomatis menonaktifkan Token yang gagal"
- `数据库集成 - 与 TokenDAO 协同工作` → "Integrasi database - Bekerja sama dengan TokenDAO"
- `Token 运行时状态（内存中）` → "Status runtime Token (dalam memori)"
- `Token 健康状态判断` → "Penentuan status kesehatan Token"
- `健康标准` → "Standar kesehatan"
- `必须是认证用户 Token (token_type = "user")` → "Harus berupa Token pengguna terautentikasi (token_type = \"user\")"
- `当前可用 (is_available = True)` → "Saat ini tersedia (is_available = True)"
- `成功率 >= 50% 或总请求数 <= 3（新 Token 容错）` → "Tingkat keberhasilan >= 50% atau jumlah total permintaan <= 3 (toleransi Token baru)"
- `注意` → "Perhatian"
- `guest Token 永远不健康` → "Token guest selalu tidak sehat"
- `unknown Token 永远不健康` → "Token unknown selalu tidak sehat"
- `验证 Token 有效性并返回类型` → "Verifikasi validitas Token dan kembalikan jenisnya"
- `待验证的 Token` → "Token yang akan diverifikasi"
- `token_type: "user" | "guest" | "unknown"` → "token_type: \"user\" | \"guest\" | \"unknown\""
- `is_valid: True 表示是有效的认证用户 Token` → "is_valid: True berarti Token pengguna terautentikasi yang valid"
- `error_message: 失败原因（仅在 is_valid=False 时有值）` → "error_message: Alasan kegagalan (hanya memiliki nilai ketika is_valid=False)"
- `解析 Z.AI 认证接口响应` → "Parse respons antarmuka autentikasi Z.AI"
- `响应格式示例` → "Contoh format respons"
- `验证规则` → "Aturan verifikasi"
- `role: "user" → 认证用户 Token（有效，可添加）` → "role: \"user\" → Token pengguna terautentikasi (valid, dapat ditambahkan)"
- `role: "guest" → 匿名用户 Token（无效，拒绝添加）` → "role: \"guest\" → Token pengguna anonim (tidak valid, tolak penambahan)"
- `其他情况 → 无效 Token` → "Kasus lain → Token tidak valid"
- `无效的响应格式` → "Format respons tidak valid"
- `未知错误` → "Kesalahan tidak diketahui"
- `Token 池管理器（数据库驱动）` → "Manajer kumpulan Token (berbasis database)"
- `Token 列表 [(token_id, token_value, token_type), ...]` → "Daftar Token [(token_id, token_value, token_type), ...]"
- `失败阈值，超过此次数将标记为不可用` → "Ambang kegagalan, melebihi jumlah ini akan ditandai sebagai tidak tersedia"
- `恢复超时时间（秒），失败 Token 在此时间后重新尝试` → "Waktu habis pemulihan (detik), Token gagal akan dicoba kembali setelah waktu ini"
- `获取下一个可用的认证用户 Token（轮询算法）` → "Dapatkan Token pengguna terautentikasi berikutnya yang tersedia (algoritma rotasi)"
- `可用的 Token 字符串，如果没有可用 Token 则返回 None` → "String Token yang tersedia, jika tidak ada Token yang tersedia maka kembalikan None"
- `获取当前可用的认证用户 Token 列表` → "Dapatkan daftar Token pengguna terautentikasi yang saat ini tersedia"
- `过滤条件` → "Kondisi filter"
- `is_available = True` → "is_available = True"
- `检测到 {guest_count} 个匿名用户 Token，轮询机制将跳过这些 Token` → "Mendeteksi {guest_count} Token pengguna anonim, mekanisme rotasi akan melewati Token ini"
- `尝试恢复失败的 Token（仅针对认证用户 Token）` → "Coba pulihkan Token yang gagal (hanya untuk Token pengguna terautentikasi)"
- `恢复了 {recovered_count} 个失败的 Token` → "Memulihkan {recovered_count} Token yang gagal"
- `标记 Token 使用成功` → "Tandai penggunaan Token berhasil"
- `重置失败计数` → "Atur ulang penghitungan kegagalan"
- `Token 恢复可用` → "Token pulih dan tersedia"
- `标记 Token 使用失败` → "Tandai penggunaan Token gagal"
- `Token 已禁用` → "Token telah dinonaktifkan"
- `获取 Token 的数据库 ID` → "Dapatkan ID database Token"
- `获取 Token 池状态信息` → "Dapatkan informasi status kumpulan Token"
- `Token 类型（用于健康检查后更新）` → "Jenis Token (untuk pembaruan setelah pemeriksaan kesehatan)"
- `更新 Token 类型` → "Perbarui jenis Token"
- `异步健康检查单个 Token（使用 Z.AI 官方认证接口）` → "Pemeriksaan kesehatan Token tunggal secara asinkron (menggunakan antarmuka autentikasi resmi Z.AI)"
- `要检查的 Token` → "Token yang akan diperiksa"
- `Token 是否健康（True = 有效的认证用户 Token）` → "Apakah Token sehat (True = Token pengguna terautentikasi yang valid)"
- `异步健康检查所有 Token` → "Pemeriksaan kesehatan semua Token secara asinkron"
- `Token 池为空，跳过健康检查` → "Kumpulan Token kosong, lewati pemeriksaan kesehatan"
- `开始 Token 池健康检查... (共 {total_tokens} 个 Token)` → "Mulai pemeriksaan kesehatan kumpulan Token... (total {total_tokens} Token)"
- `健康检查完成: 0/{total_tokens} 个 Token 健康 - 请检查 Token 配置` → "Pemeriksaan kesehatan selesai: 0/{total_tokens} Token sehat - Silakan periksa konfigurasi Token"
- `健康检查完成: {healthy_count}/{total_tokens} 个 Token 健康 ({health_rate:.1f}%)` → "Pemeriksaan kesehatan selesai: {healthy_count}/{total_tokens} Token sehat ({health_rate:.1f}%)"
- `健康检查完成: {healthy_count}/{total_tokens} 个 Token 健康` → "Pemeriksaan kesehatan selesai: {healthy_count}/{total_tokens} Token sehat"
- `个 Token 检查异常` → "Token mengalami pengecualian pemeriksaan"
- `从数据库同步 Token 状态（禁用/启用状态）` → "Sinkronkan status Token dari database (status nonaktif/aktif)"
- `提供商名称` → "Nama penyedia"
- `说明` → "Penjelasan"
- `从数据库读取最新的 Token 启用状态` → "Baca status aktivasi Token terbaru dari database"
- `如果数据库中 Token 被禁用，则从池中移除` → "Jika Token dinonaktifkan dalam database, hapus dari kumpulan"
- `如果数据库中有新增的启用 Token，则添加到池中` → "Jika ada Token aktif baru dalam database, tambahkan ke kumpulan"
- `保留现有 Token 的运行时统计（请求数、成功率等）` → "Pertahankan statistik runtime Token yang ada (jumlah permintaan, tingkat keberhasilan, dll.)"
- `从数据库加载所有启用的认证用户 Token` → "Muat semua Token pengguna terautentikasi yang diaktifkan dari database"
- `过滤 guest token` → "Filter Token guest"
- `从池中移除已禁用 Token` → "Hapus Token yang dinonaktifkan dari kumpulan"
- `添加新启用 Token` → "Tambahkan Token yang baru diaktifkan"
- `更新 Token 类型` → "Perbarui jenis Token"
- `Token 池同步完成` → "Sinkronisasi kumpulan Token selesai"
- `当前 {len(self.token_statuses)} 个 Token (移除 {len(tokens_to_remove)}, 新增 {new_tokens_count})` → "Saat ini {len(self.token_statuses)} Token (hapus {len(tokens_to_remove)}, tambah baru {new_tokens_count})"
- `从数据库初始化全局 Token 池` → "Inisialisasi kumpulan Token global dari database"
- `提供商名称 (zai, k2think, longcat)` → "Nama penyedia (zai, k2think, longcat)"
- `失败阈值` → "Ambang kegagalan"
- `恢复超时时间（秒）` → "Waktu habis pemulihan (detik)"
- `TokenPool 实例（即使没有 Token 也会创建空池）` → "Instance TokenPool (akan membuat kumpulan kosong bahkan tanpa Token)"
- `从数据库加载 Token（只加载启用的认证用户 Token）` → "Muat Token dari database (hanya muat Token pengguna terautentikasi yang diaktifkan)"
- `TokenPool 所需格式` → "Format yang dibutuhkan TokenPool"
- `过滤掉 guest token（不应该在数据库中，但防御性检查）` → "Filter Token guest (seharusnya tidak ada dalam database, tetapi pemeriksaan defensif)"
- `过滤了 {guest_count} 个匿名用户 Token` → "Memfilter {guest_count} Token pengguna anonim"
- `从数据库初始化 Token 池（{provider}），共 {len(tokens)} 个 Token` → "Inisialisasi kumpulan Token dari database ({provider}), total {len(tokens)} Token"
- `没有有效的认证用户 Token，已创建空 Token 池` → "Tidak ada Token pengguna terautentikasi yang valid, telah membuat kumpulan Token kosong"
- `将内存中的 Token 统计同步到数据库` → "Sinkronkan statistik Token dalam memori ke database"
- `应在服务关闭或定期调用，确保统计数据不丢失` → "Harus dipanggil saat layanan ditutup atau secara berkala, untuk memastikan statistik tidak hilang"
- `Token 统计已同步到数据库` → "Statistik Token telah disinkronkan ke database"

## File: app/utils/reload_config.py
- `热重载配置模块` → "Modul konfigurasi pemuatan ulang panas"
- `定义 Granian 服务器热重载时需要忽略的目录和文件模式` → "Tentukan direktori dan pola file yang harus diabaikan saat pemuatan ulang panas server Granian"

## File: app/utils/logger.py
- `日志目录` → "Direktori log"
- `日志保留天数` → "Jumlah hari retensi log"
- `日志轮转间隔` → "Interval rotasi log"
- `是否开启调试模式` → "Apakah akan mengaktifkan mode debug"
- `移除所有现有的日志处理器（支持热重载）` → "Hapus semua prosesor log yang ada (mendukung pemuatan ulang panas)"
- `无法创建日志文件 ({e})，将仅使用控制台输出` → "Tidak dapat membuat file log ({e}), hanya akan menggunakan output konsol"
- `这是一条调试日志` → "Ini adalah log debug"
- `这是一条信息日志` → "Ini adalah log informasi"
- `这是一条警告日志` → "Ini adalah log peringatan"
- `这是一条错误日志` → "Ini adalah log kesalahan"
- `这是一条严重日志` → "Ini adalah log kritis"
- `发生了除零异常` → "Terjadi pengecualian pembagian nol"
- `日志测试完成` → "Uji log selesai"
- `日志测试失败` → "Uji log gagal"

## File: app/templates/components/token_stats.html
- `Token 总数` → "Jumlah total Token"
- `已启用` → "Diaktifkan"
- `认证用户` → "Pengguna terautentikasi"
- `个匿名` → "anonim"
- `总成功率` → "Tingkat keberhasilan total"
- `暂无请求` → "Tidak ada permintaan"

## File: app/templates/components/token_pool.html
- `Token 池状态卡片` → "Kartu status kumpulan Token"
- `类型` → "Jenis"
- `认证用户` → "Pengguna terautentikasi"
- `匿名用户` → "Pengguna anonim"
- `未知` → "Tidak diketahui"
- `成功率` → "Tingkat keberhasilan"
- `失败次数` → "Jumlah kegagalan"
- `最后使用` → "Terakhir digunakan"
- `暂无 Token 配置` → "Tidak ada konfigurasi Token"
- `请在配置管理页面添加 Token` → "Silakan tambahkan Token di halaman manajemen konfigurasi"

## File: app/templates/components/token_row.html
- `单个 Token 行模板` → "Template baris Token tunggal"
- `复制完整 Token` → "Salin Token lengkap"
- `认证用户` → "Pengguna terautentikasi"
- `匿名用户` → "Pengguna anonim"
- `未知` → "Tidak diketahui"
- `健康度指示器` → "Indikator kesehatan"
- `健康` → "Sehat"
- `匿名` → "Anonim"
- `已禁用` → "Dinonaktifkan"
- `不健康` → "Tidak sehat"
- `已启用` → "Diaktifkan"
- `已禁用` → "Dinonaktifkan"
- `成功` → "Berhasil"
- `失败` → "Gagal"
- `成功率` → "Tingkat keberhasilan"
- `未使用` → "Tidak digunakan"
- `验证 Token` → "Verifikasi Token"
- `删除 Token` → "Hapus Token"

## File: app/templates/components/token_list.html
- `Token 列表表格` → "Tabel daftar Token"
- `类型` → "Jenis"
- `健康度` → "Kesehatan"
- `状态` → "Status"
- `使用统计` → "Statistik penggunaan"
- `创建时间` → "Waktu pembuatan"
- `操作` → "Operasi"
- `暂无 Token` → "Tidak ada Token"
- `点击右上角"添加 Token"按钮开始添加` → "Klik tombol "Tambah Token" di kanan atas untuk mulai menambahkan"
- `Token 已复制到剪贴板` → "Token telah disalin ke clipboard"
- `复制失败` → "Gagal menyalin"
- `请手动复制` → "Silakan salin secara manual"

## File: app/templates/components/provider_status.html
- `提供商状态详情组件` → "Komponen detail status penyedia"
- `Token 统计` → "Statistik Token"
- `总计` → "Total"
- `已启用` → "Diaktifkan"
- `认证用户` → "Pengguna terautentikasi"
- `匿名用户` → "Pengguna anonim"
- `未知类型` → "Jenis tidak diketahui"
- `请求统计` → "Statistik permintaan"
- `总请求数` → "Jumlah total permintaan"
- `成功` → "Berhasil"
- `失败` → "Gagal"
- `成功率` → "Tingkat keberhasilan"
- `快速操作` → "Operasi cepat"
- `管理 Token →` → "Kelola Token →"
- `暂无提供商状态信息` → "Tidak ada informasi status penyedia"

## File: app/templates/components/recent_logs.html
- `最近请求日志列表` → "Daftar log permintaan terbaru"
- `时间` → "Waktu"
- `端点` → "Endpoint"
- `模型` → "Model"
- `提供商` → "Penyedia"
- `状态` → "Status"
- `耗时` → "Durasi"
- `暂无请求日志` → "Tidak ada log permintaan"

## Kesimpulan
String-string Cina dalam kode sumber ini umumnya berkaitan dengan:
1. Teks UI untuk antarmuka pengguna
2. Pesan log sistem
3. Komentar dalam kode
4. Deskripsi fungsi dan modul
5. Instruksi dan dokumentasi teknis

Terjemahan ini dapat digunakan untuk membantu pengembangan internasionalisasi (i18n) aplikasi atau untuk pemahaman konten dalam kode sumber.